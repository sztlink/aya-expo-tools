# AYA Expo Tools — Computer Vision dependencies
# GPU: NVIDIA (qualquer — GTX 1080 Ti, RTX 3070, RTX 4090, etc.)
# Install: pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121

# Core
ultralytics>=8.3.60        # YOLO11 + ByteTrack tracking
opencv-python-headless>=4.9.0
numpy>=1.24.0,<2.0.0       # numpy 2.x quebra algumas dependências (ex: coremltools)

# TensorRT — opcional mas recomendado para NVIDIA GPUs
# Se TRT disponível: detector exporta PT → .engine (uma vez, cacheado) → 3-5x mais rápido
# Instalar separadamente se necessário:
#   pip install tensorrt --extra-index-url https://pypi.ngc.nvidia.com
# ou via: pip install nvidia-tensorrt
# (já incluso em alguns ambientes CUDA — verificar com: python -c "import tensorrt")

# Não incluído aqui para não quebrar instalação em máquinas sem CUDA:
# tensorrt
